import { colors } from "@/theme/colors"
import { FONT_FAMILY, fontFamily } from "@/theme/fontFamily"
import { fontSize } from "@/theme/fontSize"

export {colors} from "@/theme/colors"

export const theme = {
    colors: colors,
    fontFamily: fontFamily,
    fontSize: fontSize
}
